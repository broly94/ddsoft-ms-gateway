import { RpcExceptionInterceptor } from './rpc-exception.interceptor';

describe('RpcExceptionInterceptor', () => {
  it('should be defined', () => {
    expect(new RpcExceptionInterceptor()).toBeDefined();
  });
});
